"""wake_train_kit - shared helpers for WakeStudio module train scripts.

Deliberately tiny for v1: the NDJSON reporter (the service contract, see
docs/modules/training.md 4.4). TTS sample-gen, augmentation and artifact
bundling helpers land here as modules gain train targets (issue #121).
"""
