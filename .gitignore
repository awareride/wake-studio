# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
yarn.lock

# Logs
logs
*.log

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# Build / cache
.cache/
dist/
build/
.packscope-cache/
.astro/

# Local environment
.env
.env.local

# Archive (old static site)
_archive/
.wrangler/

# Lockfiles (pnpm is canonical; ignore npm/yarn)
package-lock.json

# TypeScript build info
*.tsbuildinfo
node_modules/.tmp/

# Playwright
playwright-report/
test-results/

# Agents
.agents/

# Pre-fetched local assets (ADR-011 amendment; dev-only, not committed)
apps/web/prebuilts/

# sherpa-onnx KWS WebAssembly runtime (built by .github/workflows/build-sherpa-onnx-kws-wasm.yml).
# Dev-only / not committed; fetch with `node scripts/fetch-sherpa-kws-assets.mjs`.
apps/web/public/sherpa-onnx-kws/

# Module-owned binary assets (ADR-025). Built in CI (ADR-027), fetched locally,
# never committed (ADR-011).
packages/modules/*/*/assets/*
!packages/modules/*/*/assets/README.md
