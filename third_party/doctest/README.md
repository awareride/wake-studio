# doctest — vendored test framework

- Upstream: https://github.com/doctest/doctest
- Version: **v2.4.11** (MIT)
- Import: pristine single header (`doctest/doctest.h`), no patches.
- Purpose: L1 unit + composition-root integration tests for the device-side
  SDK (`device/tests/`), per ADR-026 applied to the C/C++ world.
- Upgrade: replace the header with a newer release tag and note it here.
