// Side-effect module: installs `atob` and `self.location` polyfills required by
// the vendored RNNoise WASM glue (generated/rnnoise-sync.js) inside an
// AudioWorkletGlobalScope, where they are not available natively.
export {}
